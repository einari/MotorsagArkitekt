/// <reference types="vite/client" />
// Pulls in Vite's ambient types: `import.meta.env` (incl. BASE_URL) and asset
// module declarations such as *.ttf used by src/text/fonts.ts.
// Named global.d.ts so it survives the repo's `*.d.ts` gitignore rule.
